import { Feather } from '@expo/vector-icons'

export const icon = {
    index: (prop: any) => <Feather name="home" size={24} />,
    chart: (prop: any) => <Feather name="bar-chart-2" size={24} />,
    setting: (prop: any) => <Feather name="settings" size={24} />,
}